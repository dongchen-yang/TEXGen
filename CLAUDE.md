# CLAUDE.md

Agent instructions for this submodule live in the parent lightgen repo as a path-scoped rule,
`lightgen/.claude/rules/texgen_emission.md`, which loads whenever a file under `TEXGen-Emission/`
is read from a session started at the lightgen root. Repo-wide conventions: `lightgen/AGENTS.md`;
the doc of record is `TEXGEN_EMISSION.md`.
