# CLAUDE.md

Agent instructions for this submodule live in the parent lightgen repo as a path-scoped rule,
`lightgen/.claude/rules/texgen.md`, which loads whenever a file under `TEXGen/` is read from a
session started at the lightgen root. Repo-wide conventions: `lightgen/AGENTS.md`;
the workflow map stays here as `LIGHTGEN_WORKFLOW.md`.
